<Page
    x:Class="PowerRename.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerRename"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ToolkitControls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
 
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    xmlns:converters="using:Microsoft.Toolkit.Uwp.UI.Converters"
    Background="Transparent"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <Page.Resources>
        <converters:EmptyObjectToObjectConverter x:Key="NullToVisibilityConverter" EmptyValue="Collapsed" NotEmptyValue="Visible"/>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter"/>

    </Page.Resources>
    <Grid Background="Transparent">

        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" x:Name="PreviewColumn"/>
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}"
                                VerticalAlignment="Stretch" 
                                HorizontalAlignment="Stretch" 
                                Margin="16"
                                Padding="16"
                                CornerRadius="8"
                                BorderThickness="1"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
        </Grid>


        <!--<TextBlock Text="Preview results"
                   FontWeight="Bold"
                   FontSize="16"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   VerticalAlignment="Center"
                   Margin="12,24,0,0"/>-->

        <!--<TextBlock Text="Rename criteria"
                   FontWeight="Bold"
                   FontSize="16"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   VerticalAlignment="Center"
                   Margin="12,48,0,0" />-->

        <Grid Grid.Column="1"  Grid.Row="1" Background="{ThemeResource LayerFillColorDefaultBrush}"
                                VerticalAlignment="Stretch" 
                                HorizontalAlignment="Stretch" 
                                Margin="16"
                              
                                CornerRadius="8"
                                BorderThickness="1"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">

            <StackPanel Orientation="Vertical" Padding="24,16,24,16">
                <TextBox>
                    <TextBox.Header>
                        <TextBlock Text="Search for" FontWeight="SemiBold"/>
                    </TextBox.Header>
                </TextBox>
                    <CheckBox Content="Use RegEx" Margin="0,4,0,0" />
                    <CheckBox Content="Match all occurences" />
                    <CheckBox Content="Case sensitive" />

                <Rectangle Height="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" HorizontalAlignment="Stretch" Margin="-24,16,-24,16" />
                <TextBox Margin="0,0,0,32">
                    <TextBox.Header>
                        <TextBlock Text="Replace with" FontWeight="SemiBold"/>
                    </TextBox.Header>
                </TextBox>

                    <TextBlock FontWeight="SemiBold" Text="Inclusions"/>

                                <CheckBox Content="Use RegEx" />
                                <CheckBox Content="Match all occurences" />
                    <CheckBox Content="Case sensitive" />

                    <TextBlock FontWeight="SemiBold" Text="Replace" Margin="0,16,0,0"/>
                    <ComboBox SelectedIndex="0">
                        <x:String>Entire file name</x:String>
                        <x:String>File name only</x:String>
                        <x:String>File extension only</x:String>
                    </ComboBox>
                    <TextBlock FontWeight="SemiBold" Text="Format" Margin="0,16,0,0"/>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <ToggleButton Content="Tt"/>
                        <ToggleButton Content="TT"/>
                        <ToggleButton Content="Tt"/>
                        <ToggleButton Content="Tt"/>
                    </StackPanel>
                    <CheckBox Content="Enumerate items" />

                    <StackPanel Orientation="Horizontal" Grid.Row="5" Spacing="12" Margin="12" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                        <Button x:Name="SettingsBtn" Grid.Row="5" Background="Transparent" Grid.Column="1">
                            <Button.Content>
                                <FontIcon Glyph="&#xE713;" />
                            </Button.Content>
                        </Button>
                        <Button Style="{StaticResource AccentButtonStyle}">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE8AC;" FontSize="14" FontFamily="Segoe MDL2 Assets" />
                                    <TextBlock Text="Rename"/>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>

                </StackPanel>
        </Grid>




        <!--<ListView ItemsSource="{x:Bind SearchRegExShortcuts}"
                                          Grid.Row="1"
                                          IsItemClickEnabled="True"
                                          ItemClick="ListView_ItemClick"
                                          Margin="-4,12,0,0">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:RegExShortcut">
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="-8,0,0,0">
                                        <ToolkitControls:DropShadowPanel ShadowOpacity="0.2" OffsetX="0" OffsetY="1" BlurRadius="2">
                                            <Border CornerRadius="2" Padding="8" Background="{ThemeResource SnippetBackgroundBrush}">
                                                <TextBlock FontFamily="Consolas" Text="{x:Bind Code}"/>
                                            </Border>
                                        </ToolkitControls:DropShadowPanel>
                                        <TextBlock Text="{x:Bind Description}" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>-->

        <StackPanel x:Name="TitleBar" Orientation="Horizontal">
            <Image Source="Assets/PowerRename.png" Width="16" Height="16" VerticalAlignment="Top" Margin="11,9,0,0"/>
            <TextBlock Text="PowerRename" Style="{StaticResource CaptionTextBlockStyle}" Margin="8,8,0,0" VerticalAlignment="Top" />
        </StackPanel>
    </Grid>
</Page>